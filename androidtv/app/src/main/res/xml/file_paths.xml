<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The downloaded update APK lives in cacheDir; FileProvider shares it with
         the package installer. -->
    <cache-path name="cache" path="." />
</paths>
