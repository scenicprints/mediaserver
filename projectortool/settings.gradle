pluginManagement {
    repositories { google(); mavenCentral(); gradlePluginPortal() }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        // GeckoView is published by Mozilla, not to Central.
        maven { url 'https://maven.mozilla.org/maven2/' }
    }
}
rootProject.name = 'ProjectorTool'
include ':app'
include ':marquee'
